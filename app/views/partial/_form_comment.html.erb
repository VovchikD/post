<%= form_for([@post, @post.comments.build]) do |f| %>
<%= f.hidden_field :parent_id, value: nil %>
  <%= f.text_area :content, placeholder: 'Your comment...' %>
  <%= f.submit 'Add comment', class: 'btn btn-sm btn-outline-secondary' %>
<% end %>